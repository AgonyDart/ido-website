const $ = (id) => document.getElementById(id);

// => Homepage
const homeWelcome  = $('home-welcome');
const homeHero = $('home-hero');
const homeSections  = $('home-sections');
const homeMembers  = $('home-members');

const glosarioSection  = $('glosario');

const networkSection  = $('networking');
const pdfNode = $('pdf');

const programSection  = $('program');